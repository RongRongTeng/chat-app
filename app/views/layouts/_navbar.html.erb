<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <a class="navbar-brand" href="<%= root_path %>">Chat App</a>
  <% if current_user %>
    <ul class='navbar-nav ml-auto'>
      <%= link_to 'Logout', destroy_user_session_path, data: { turbo_method: :delete }, class: 'btn btn-outline-info' %>
  <% end %>
  </ul>
</nav>
